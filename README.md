# Lumi

The Lumi frontend and backend are being refactored away from Firebase toward a FastAPI + PostgreSQL + Tencent COS stack on Ubuntu/Caddy. This top-level README will be updated once the migration stabilizes.

For backend setup and current API details, see `functions/README.md`.
